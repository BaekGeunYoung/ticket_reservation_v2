kind: Ingress
apiVersion: extensions/v1beta1
metadata:
  name: ingress
spec:
  rules:
    - http:
        paths:
          - path: /api/*
            backend:
              serviceName: ticket-reservation-api
              servicePort: 80